PORT=8081
BASE_PATH=/
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_KEY=your-anon-key-here

# Pre-listing validator compute API (HSN / CLIP / text correction).
# Point this at your local Cloudflare Tunnel when deploying the frontend.
# Leave unset to default to http://localhost:8080 (local dev).
VITE_PRELISTING_API_URL=https://your-tunnel.trycloudflare.com
